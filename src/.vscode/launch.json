{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Main.py (in src)",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/src/main.py",
      "cwd": "${workspaceFolder}/src"
    }
  ]
}
